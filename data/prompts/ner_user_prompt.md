Estas son demostraciones curadas de cómo aplicar el codebook. Estudia el patrón de decisión, pero no copies spans de las demostraciones si no aparecen literalmente en el fragmento real.

<few_shot_examples>
$few_shot_examples
</few_shot_examples>

Ahora analiza exclusivamente el campo `text` del siguiente objeto. Recorre el fragmento completo, extrae todas las apariciones válidas según el codebook y entrega el resultado mediante `submit_entity_annotations`.

<target_input>
$json_input
</target_input>
