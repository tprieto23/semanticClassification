# API
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Base de datos
sqlalchemy==2.0.25
alembic==1.13.1
psycopg2-binary==2.9.9
pgvector==0.2.4

# Utilidades
python-dotenv==1.0.0
pydantic>=2.5.3
pydantic-settings>=2.3.0

# Conversión de documentos (Objetivo 2)
docling
pymupdf4llm
PyMuPDF>=1.24.0

# Limpieza de textos (Objetivo 3)
ftfy==6.2.3
nltk

# NER / Extracción de entidades (Objetivo 4)
anthropic>=0.49.0
rapidfuzz>=3.9.0,<4.0.0

# torch/torchvision: requeridos por docling/transformers.
# Se instalan CPU-only en el Dockerfile (evita wheels CUDA nvidia ~1.5GB).
# torch        ← instalado en Dockerfile desde índice CPU
# torchvision  ← instalado en Dockerfile desde índice CPU
transformers   # requerido por docling (import a nivel de módulo)

# Fine-tuning NER con XLM-RoBERTa (Objetivo 4) — OMITIDO por ahora (pesado)
# datasets
# seqeval
# accelerate
# evaluate
